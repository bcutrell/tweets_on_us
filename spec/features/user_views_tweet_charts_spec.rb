require 'spec_helper'

feature 'tweeter sees chart of tweet numbers', %Q{
  As a visually inclined user
  I can see a graph of tweets by group
  So that I can see which group is the most active
  } do

  scenario 'user sees group tweets chart' do
    # user = FactoryGirl.create(:administrator)
    # login_with_oauth
    # click_on 'Admin'

    # visit groups_path
    # expect(page).to have_content("Tweets by Group")
  end
  
end
